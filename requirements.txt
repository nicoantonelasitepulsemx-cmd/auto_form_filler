playwright>=1.44.0

# Optional — only needed when running with --captcha-solver=audio (the
# Buster-style reCAPTCHA v2 bypass). The first audio solve will download
# the tiny.en model (~75 MB) into ~/.cache/huggingface.
faster-whisper>=1.0.0
